ready: false
issues:
  - severity: structural
    summary: The new design role config omits how judge and reviewer models resolve to executable adapters.
    detail: Phase 2 extends the config schema to support `{"pattern": "design_loop", "judge": {"model": ...}, "reviewer": {"model": ...}, "max_rounds": N}` and later says the default judge is `opus` and the default reviewer is `codex`. The existing Orchestra role-binding surface requires an adapter as well as a model, and `run_role` must build executable actor bindings for the workflow. The plan does not specify whether the nested `judge` and `reviewer` entries must include adapters, whether `opus` and `codex` are symbolic bindings resolved through a registry, or whether `run_role` applies hard-coded default adapters. An implementer would have to invent that resolution contract to make Phase 2 executable and to let Phase 3 call `orchestra.run_role("design", ...)` reliably.
rationale: The phase ordering is now coherent: Phase 1 is independent, and Phase 3 is correctly declared dependent on Phase 2. The judge-first workflow also captures the required behavior at the loop level: the judge produces artifacts, the reviewer critiques only, and the judge alone decides convergence, with first-turn `done`, malformed output, cap, and same-model cases covered by tests. The remaining issue is structural because the task list still lacks the adapter/model binding mechanics needed to run the new role config, so the plan is not yet complete enough for implementation without inventing an unstated subtask.
