## Phase phase_001: Establish package boundaries and acceptance infrastructure

- [ ] Create `Package.swift` for macOS 14 and initial arm64 builds, with the accepted library targets, runtime adapters, service executables and application shell. Keep Core and Ports independent of platform frameworks, SQLite and inference types.
- [ ] Define the authoritative application values and port signatures in `Sources/Ports`, including source timing, runtime capabilities, context omissions, delivery plans, store publication and opaque lease handles. Preserve the accepted IPC and lifecycle interfaces.
- [ ] Add versioned serialization fixtures for manifests, journal records and IPC values. Reject unsupported versions and invalid lengths before allocating payload storage.
- [ ] Create `docs/implementation-map.md` mapping D-001 through D-043, I1 through I8 and every requested feature to implementation ownership and acceptance evidence. Reference the accepted design without copying competing protocol definitions.
- [ ] Create `docs/acceptance/state-events.md` with specification-derived transitions, durable effects and visible outcomes. Include cancellation at every stage, deletion/publication races, uncertain delivery and interrupted recovery before deriving assertions.
- [ ] Create `scripts/acceptance.py` with named suites, evidence directories and machine-readable results. Exit 0 only when the requested checks meet their stated outcomes, 1 for failed assertions and 2 for missing prerequisites or inconclusive evidence. Never count skipped hardware checks as passing.
- [ ] Add fake clocks, process identities, sources, workers, targets and fault-injectable filesystem operations under `Tests/Support`. Keep fixture data local and prevent test helpers from modifying the user's library.
- [ ] Add `Tests/ArchitectureTests` and the `foundation` acceptance suite to check package dependency direction, serialization rejection and dispatcher failure behavior.
- [ ] [AUTO:run_cli] Run the foundation suite. Expect successful compilation, prohibited imports rejected by the architecture check, and malformed fixtures rejected without oversized allocation. [accept: command-exit: python3 scripts/acceptance.py foundation]
- [ ] [USER] Have an engineering reviewer examine the state/event expectations against the specification and accepted design. Record disagreements before dependent assertions are approved. A passing suite establishes only the tested assertions.

## Phase phase_002: Implement persistent ownership and publication

- [ ] Implement versioned GRDB migrations in `Sources/Store` for the complete accepted logical schema, including sessions, audio representations, runs, prepared output, result dependencies, speaker revisions, settings, context, delivery and model ownership.
- [ ] Configure one SQLite writer with WAL and `synchronous=FULL`. Add paged readers, bounded caches and hidden preparation batches capped at 256 rows, yielding between batches and reducing batch size when measured writer occupancy exceeds 10 ms.
- [ ] Implement canonical `RunManifest` persistence and immutable result publication. Synchronize manifest bytes before references, verify dependencies, and atomically check owner generation plus execution epoch when exposing prepared output. [feat: "History"]
- [ ] Implement `ArtifactBroker` and `LeaseBroker` eligibility for live session creators, registered sessions, recordings, installed models and frozen staging execution. Give setup smoke tests and idle preview model ownership independent of recordings. [feat: "Models"]
- [ ] Implement physical asset references, registered creators and publication claims. Pin acquired manifests and their reachable audio bytes, and pin actual model blobs and materializations for active model users.
- [ ] Implement reclamation by snapshotting retention roots and broker revision, scanning manifests in bounded pages, then marking assets deleting only after an unchanged-revision eligibility check. Missing caches must cause scanning or deferred reclamation.
- [ ] Implement tombstones, generation increments and compact deletion records before physical cleanup. Exclude new dependent work immediately, fence publication and retain only the detached delivery metadata required for recovery. [feat: "History"]
- [ ] Establish `Application Support/LocalDictation` roots with current-user permissions and validated relative paths. Keep CaptureStore independent of database scheduling and reserve `Sessions/<sessionID>` as the permanent owner directory.
- [ ] Add `Tests/OwnershipTests` and the `ownership` suite for late publication, concurrent acquisition/removal, shared references, live creators, missing caches and interruption during migration or publication.
- [ ] [AUTO:run_cli] Run ownership acceptance. Expect no publication from discarded or tombstoned owners, no deletion of leased/shared bytes, and no partially prepared result visible after interruption. [accept: command-exit: python3 scripts/acceptance.py ownership]

## Phase phase_003: Implement run coordination and resource admission

- [ ] Implement `RunActor` with the accepted processing states, immutable input snapshots, execution epochs and terminal timestamps. Only completed, cancelled and failed runs receive `ended_at`; suspended work remains resumable under its stage contract.
- [ ] Implement `ResourceScheduler` with the provisional 8 GiB budget and D-027 charges. Reserve 512 MiB for shell/Store, 128 MiB for capture, 1 GiB for preview and 2 GiB for final speech while recording controls are enabled.
- [ ] Charge generation configurations by their complete envelope, initially at most 4 GiB. Add 512 MiB decode/compression, 256 MiB materialization and 1.5 GiB diarization charges. Keep compatibility separate from admission. [feat: "Models"]
- [ ] Implement final-recognition window scheduling with at most two latest-live windows before one oldest-waiting window. Preserve the quota across new dictations and model changes, removing completed or cancelled runs by enqueue sequence.
- [ ] Implement ordinary checkpoint preemption and resource release after verified unload or process exit. A disconnected worker loses completion authority immediately; its memory charge remains until release is established.
- [ ] Implement stage-specific cancellation routing under D-007, including durable discard during sealing, preservation of committed recognition during cleanup cancellation, and retention of generation inputs. Report persistence failures without acknowledging uncommitted cancellation. [feat: "Recording controls"]
- [ ] Implement worker crash handling without automatic recognition or generation retry. Two consecutive crashes for the same installation and parameters mark it suspect; successful completion resets the counter. Explicit retry creates a new run. [feat: "Models"]
- [ ] Add `Tests/SchedulerTests` and the `scheduling` suite with finite fake windows, quota-boundary arrivals, model switches, oldest-run cancellation, delayed worker exits and resource denial.
- [ ] [AUTO:run_cli] Run scheduling acceptance. Expect the oldest waiting run to complete a window after at most two latest-live windows during continued admission, stale epochs to be rejected, and unreleased workers to remain charged. [accept: command-exit: python3 scripts/acceptance.py scheduling]

## Phase phase_004: Build recoverable capture storage

- [ ] Implement `CaptureSeed` and append-only `SessionControl` in `Sources/CaptureStore`. Protect directory construction with the live-instance creator before journal creation, then transfer eligibility only after database association commits. [feat: "Recording controls"]
- [ ] Implement preallocated 60-second rings per stream, with backlog measured through the last journal-committed frame. Keep ring slots occupied through commit and prohibit an additional uncommitted audio queue. [feat: "Input sources"]
- [ ] Implement approximately two-second native-frame chunks. Latch pending microphone gain at each chunk's first admitted frame, apply it once to Float32 ring samples, and preserve the latched value through delayed writes and retries. [feat: "Desktop behavior"]
- [ ] Implement versioned saturation and signed 24-bit quantization, native timing, channel layout and saturated-frame counts. System audio and imports use unity gain. Preview consumes adjusted live samples; playback never reapplies gain. [feat: "Input sources"]
- [ ] Implement `.part` writing, file synchronization, no-replace rename, directory synchronization and checksummed journal append/synchronization before releasing frames. Limit durability claims to application interruption with an operational OS and filesystem. [feat: "History"]
- [ ] Implement incremental `RepresentationManifest` construction with independently decodable entries, hashes, native frame counts, timeline mapping and a paged offset index. Keep the manifest authoritative and database audio caches rebuildable. [feat: "History"]
- [ ] Implement backlog responses at ten seconds, forty-five seconds and sixty seconds. Suspend competing application disk work at ten, request stop at forty-five, and refuse further admission before exceeding sixty seconds even if control execution is delayed. [feat: "Recording controls"]
- [ ] Implement sealing and discard/publication arbitration. A durable discard forbids publication; if publication wins first, finish cancellation through recording deletion. Recovery follows committed disposition before interruption preferences. [feat: "Recording controls"]
- [ ] Implement verified-prefix recovery, including complete adjacent chunks, torn-tail rejection and database-authoritative publication after an interrupted file-marker update. Recover eligible live audio into an explicit Process-or-Discard state. [feat: "History"]
- [ ] Add `Tests/CaptureDurabilityTests` and the `capture-storage` suite with fault injection at every write boundary, repeated interruption during recovery, known PCM and writer stalls of two, ten, thirty and over sixty seconds.
- [ ] [AUTO:run_cli] Run capture-storage acceptance. Expect healthy-storage prefixes without leading-audio loss, admitted-frame preservation when stalls clear before the stop threshold, and outstanding audio never exceeding sixty seconds per stream. [accept: command-exit: python3 scripts/acceptance.py capture-storage]
- [ ] [AUTO:run_cli] Run gain fixtures, including edits during a thirty-second backlog. Expect old admitted chunks to retain old gain, the next admitted chunk to use the latest pending gain, exact specified quantization and no double application on retry. [accept: command-exit: python3 scripts/acceptance.py capture-gain]

## Phase phase_005: Establish signed services and materialization

- [ ] Create `packaging/Info.plist`, reviewed entitlement files and `scripts/package-app.sh`. Assemble SPM products into the shared development/release bundle layout and sign embedded services before the containing application.
- [ ] Implement separate PreviewService, RecognitionService, GenerationService, DiarizationService and DecodeService entry points. Enable their sandboxes without network client/server entitlements and authenticate XPC peers by code identity.
- [ ] Implement `IPCEnvelope` validation with protocol version, job ID, epoch, input hashes and payload length. Limit audio messages to two seconds with four outstanding messages per stream; limit text and metadata batches to 64 KiB.
- [ ] Implement credited input consumption and durable output handoff through read handles. Register service scratch before creation, verify returned lengths and hashes, copy accepted output into owner staging, then acknowledge durable receipt.
- [ ] Implement materialization keys from set hash, service kind, runtime revision and cache-format revision. Protect temporary copies with creator leases and publish synchronized verified materializations under per-key no-replace claims. [feat: "Models"]
- [ ] Implement materialization accounting and retirement. Allow one current copy per consuming service and one temporary replacement per key; retain old caches until holders drain and protect shared weights across installations. [feat: "Models"]
- [ ] Implement a separate ModelDownloader service interface accepting only an approved acquisition URL and staging capability. Give inference workers no downloader connection or library-root access. [feat: "Models"]
- [ ] Add the `service-boundary` suite with signed probe services for both network directions, unrelated-library access, peer rejection, malformed messages, credit exhaustion and interruption before/after output acknowledgment.
- [ ] [AUTO:run_cli] Run the ad hoc signed service suite. Expect denied networking and unrelated-library access, bounded transport queues, stale responses rejected and acknowledged output retained in app ownership. Missing sandbox enforcement blocks this phase. [accept: command-exit: python3 scripts/acceptance.py service-boundary --signing adhoc]

## Phase phase_006: Integrate local runtimes and model management

- [ ] Pin reviewed WhisperKit/SpeakerKit and llama.cpp revisions, with reproducible native bridge builds. Inspect path loading and retained allocations, and record runtime/adapter revisions in capability descriptors. [feat: "Models"]
- [ ] Implement WhisperKit adapters for separate preview and final services using explicit local paths with acquisition disabled. Implement bounded window result contracts, optional language evidence and declared token/segment timing precision. [feat: "Models"]
- [ ] Implement llama.cpp GGUF loading in GenerationService with explicit tokenizer, chat template, context length and KV cache. Exclude server tooling and HTTP inference endpoints. [feat: "Models"]
- [ ] Implement the SpeakerKit window adapter with explicit `modelFolder`, `download=false`, and segmenter/embedder concurrency of one. Return optional centroids and represent unavailable evidence as unknown. [feat: "Meetings"]
- [ ] Implement model staging before acquisition, regular-file/path validation, canonical asset manifests and hashes covering weights plus behavior-affecting companions. Reject executable plugins, symlinks and paths escaping the staging root. [feat: "Models"]
- [ ] Implement frozen-staging execution leases, structural validation and signed-service smoke tests. Accept valid empty speech/diarization output and immediate generation end-of-sequence without classifying them as incompatibility. [feat: "Models"]
- [ ] Implement set-hash publication claims, verification of concurrent winners, synchronized no-replace blob publication and transactional installation activation. Failed acquisition or smoke testing must preserve the working active installation. [feat: "Models"]
- [ ] Implement explicit user-requested downloads with redirect validation and exact catalog hashes. Retain failed staging for seven days or dismissal, subject to live ownership, and inventory notices for each distributed component. [feat: "Models"]
- [ ] Build model browsing, local import, removal and archival UI. Show declared/evaluated language coverage, unmeasured quality, hardware limits and storage by unique/shared weights, staging and service caches. [feat: "Models"]
- [ ] Implement installed, archived, orphaned_runtime and missing states. Preserve historical parameter descriptions and notices, drain old workers during runtime updates, and require compatible assets for restored execution. [feat: "Models"]
- [ ] Add `Tests/ModelLifecycleTests` and the `models-f2` suite for all runtime families, concurrent identical imports, interrupted activation, shared-cache archival, smoke testing without recordings and implicit-download attempts.
- [ ] [AUTO:run_cli] Run F2 with the selected local assets through ad hoc signed services. Expect contract-valid compatible imports to remain selectable without quality scores, old installations to survive failures, and every inference network attempt to fail. Do not accept adapters without this evidence. [accept: command-exit: python3 scripts/acceptance.py models-f2 --signing adhoc]

## Phase phase_007: Implement mode revisions, vocabulary and context

- [ ] Implement immutable mode, vocabulary and rule revisions plus the trigger-time settings registry. Persist `ModeContract` fields for instructions, permitted operations, question answering, language, context, model choices and unavailable-check policy. [feat: "Custom modes"]
- [ ] Add read-only Standard, Email, Note and Meeting notes definitions under D-013. Set `answersQuestions=false` for each, preserve their distinct transformation permissions, and create custom revisions when users customize a built-in. [feat: "Built-in modes"]
- [ ] Build the custom mode editor for prompts, formatting, local model selection and explicit question-answering behavior. Show compatibility and missing quality evidence without requiring a validator pack to execute a compatible model. [feat: "Custom modes"]
- [ ] Implement ordered rule resolution from the frozen trigger registry. Per-mode shortcuts win, then the first matching application/website rule, then the default. Show shadowed rules and the effective mode's selection reason. [feat: "Automatic mode selection"]
- [ ] Implement bounded URL sampling with a 200 ms deadline, supported browser host/path predicates, and unavailable-input fallback. Exclude query strings and fragments by default and ignore later focus changes for mode selection. [feat: "Automatic mode selection"]
- [ ] Implement vocabulary bias ordering, deduplication and tokenizer budgeting. Report omitted complete terms; adding terminology must carry no guarantee of recognition accuracy. [feat: "Vocabulary"]
- [ ] Implement one-pass longest-match replacements with list-order ties, locale-aware whole-word options, optional standalone filler removal and designated output replacements. Preserve raw recognition and prevent replacement cascades. [feat: "Vocabulary"]
- [ ] Implement optional application-text and clipboard sampling within 200 ms, capped at 32 KiB per source. Return typed omissions for permission denial, secure input, expiry and unavailable identities. [feat: "Context"]
- [ ] Implement isolated foreground-window capture and local Vision OCR within one second, capped at four megapixels. Handle spanning-display scale normalization and omit changed, closed or minimized windows without display fallback. [feat: "Context"]
- [ ] Implement subordinate context representations, extracted terms by default and optional prose. Release pixels after OCR; retain only status/time/digest by default and the exact supplied representation plus source descriptor when opted in. [feat: "Context"]
- [ ] Add the `settings-context` suite with registry edits during capture, replacement overlap, URL timeout, unavailable permissions and multi-display fixtures containing unrelated text beside and behind the selected window.
- [ ] [AUTO:run_cli] Run settings/context acceptance. Expect frozen settings, deterministic fallback and cleanup order, excluded unrelated pixels/OCR, ignored late samples, and no retained pixels or context payloads in logs/search. [accept: command-exit: python3 scripts/acceptance.py settings-context]

## Phase phase_008: Connect live dictation and provisional display

- [ ] Implement on-demand AVAudioEngine microphone input and ScreenCaptureKit system input through `AudioSource`. Negotiate rates/channels against capture charges and keep callbacks free of allocation, inference, database work and target lookup. [feat: "Input sources"]
- [ ] Connect session acquisition to the shell. Start input into preallocated storage independently of model readiness and optional context; show Starting until the first admitted buffer, then Recording and the configured cue. [feat: "Recording controls"]
- [ ] Implement configurable toggle and push-to-talk shortcuts with per-mode variants and conflicts. Stop active dictation without changing its frozen mode, and sample modifier submission intent at stop/release. [feat: "Recording controls"]
- [ ] Implement cancellation disabled by default, with optional explicit Escape binding. Consume eligible key-down/repeats and paired key-up, forward modifiers and unmatched events, and limit selected-operation cancellation to this app being frontmost at key-down. [feat: "Recording controls"]
- [ ] Implement missed-release polling, tap-disable recovery, sleep/lock handling and stale gesture cleanup without a maximum hold duration. Preserve menu controls when global-input permission is unavailable. [feat: "Recording controls"]
- [ ] Implement the movable nonactivating AppKit panel with waveform, effective mode, provisional text and stage errors. Remember display-relative placement and clamp it after display removal or visible-frame changes. [feat: "Recording display"]
- [ ] Connect bounded rolling preview, warm residency while controls are enabled and visible cold-loading after eviction. Keep final speech for the provisional sixty-second idle linger and unload idle generation/diarization. [feat: "Recording display"]
- [ ] Implement explicit recognition language, supported detection and a selected fallback. Label tentative preview language, resolve final language independently, and enter `awaiting_language` when final evidence and fallback are unusable. [feat: "Languages"]
- [ ] Implement final recognition with 30-second windows and 25-second steps, midpoint ownership and boundary diagnostics. Commit app-owned checkpoints before worker acknowledgment and publish a complete source transcript only after coverage finishes. [feat: "History"]
- [ ] Connect ordinary recognition preemption to checkpoint continuation with a new epoch. Crash interruption fails the run and retains audio; explicit retry creates a new run. A language selection replaces the manifest/run. [feat: "Languages"]
- [ ] Add the `live-dictation` suite with permission transitions, gain changes, provisional/final disagreement, cancellation in each active stage and event ownership across focus changes.
- [ ] [AUTO:run_cli] Run live-dictation acceptance. Expect frozen modes, distinct provisional/final output, no implicit input while idle, one seal per stop, durable discard on cancellation and retained audio when language selection is required. [accept: command-exit: python3 scripts/acceptance.py live-dictation]
- [ ] [AUTO:run_cli] Run shortcut fixtures. Expect background History work to survive Escape in another app, explicitly bound live-dictation cancellation to consume its gesture, and owned key-up to remain consumed after focus changes. [accept: command-exit: python3 scripts/acceptance.py shortcut-ownership]

## Phase phase_009: Add owned audio and video imports

- [ ] Implement `DecoderProvider` probing and track enumeration in DecodeService, using actual container/codec data. Keep new decoder implementations compiled into the service and reusable through the accepted `AudioSource` contract. [feat: "Input sources"]
- [ ] Implement the initial matrix for WAV PCM/IEEE float, AIFF PCM, CAF PCM/ALAC, MP3, M4A AAC/ALAC, and MOV/MP4/M4V with supported AAC/ALAC/PCM tracks. Report unsupported codecs and DRM explicitly. [feat: "Input sources"]
- [ ] Implement import probing and incremental source copying under session ownership, using an external read-only handle. Compare source identity, size and modification metadata around copying and reject observed changes. [feat: "Input sources"]
- [ ] Verify copied length/hash before entering `import_decoding`. Preflight storage for both the owned source and decoded chunks, and identify the copied byte sequence as the imported source. [feat: "Input sources"]
- [ ] Decode incrementally into native-rate session-owned chunks with unity gain. Require explicit track selection when ambiguous and complete-input validation before sealing or recording publication. [feat: "Input sources"]
- [ ] Implement import recovery under D-031. Discard incomplete probing/copying, retain verified copies after decode interruption, remove partial decoded output and offer Restart decoding. Finish sealing only with complete-input evidence. [feat: "Input sources"]
- [ ] Integrate import progress, cancellation, storage estimates and recovered actions into the application. Reuse existing publication, processing and deletion paths. [feat: "Input sources"]
- [ ] Add baseline format fixtures and the `imports` suite covering every advertised combination, corrupt tails, ambiguous tracks, source mutation, disk exhaustion and interruption at every import stage.
- [ ] [AUTO:run_cli] Run import acceptance. Expect bounded decoding, byte-identical external files after success or failure, no truncated recording publication, and the specified source-selection or Restart decoding recovery action. [accept: command-exit: python3 scripts/acceptance.py imports]

## Phase phase_010: Compile delivery targets and routes

- [ ] Implement trigger-time `TargetSnapshot` acquisition with process launch identity, window/element evidence and selection under the 500 ms watchdog. History delivery acquires its destination at the explicit action. [feat: "Text delivery"]
- [ ] Implement same-element default delivery and explicit per-mode follow-field policy within the latched application. Hold pre-effect mismatches and require Resume delivery or Deliver here to revalidate identity and selection. [feat: "Text delivery"]
- [ ] Implement immutable `TargetProfile` and evidence records with app/signing identity, OS/version ranges, field/input-method predicates, destination equivalence, required attributes and content mappings. [feat: "Text delivery"]
- [ ] Implement persistent and one-shot `UserRoutePreference` records, visible unverified status and revocation. Remembered choices never acquire evaluated status through authorization alone. [feat: "Text delivery"]
- [ ] Implement route selection before any effect. Prefer qualified settable AX selected text, then applicable Unicode profiles or explicit user routes. Prohibit AXValue insertion and automatic mechanism fallback after attempted effects. [feat: "Text delivery"]
- [ ] Implement full-output streaming preflight and disk-backed operation plans with composed-character boundaries and profile operation limits. Hold the entire result if any content lacks a supported mapping. [feat: "Text delivery"]
- [ ] Implement explicit CRLF/CR/LF/Unicode-separator and control mappings. Generic Unicode remains control-free single-line; verified break sequences must not submit. Generic paste remains available with disclosed target-specific paragraph behavior. [feat: "Text delivery"]
- [ ] Implement version drift and profile conflicts. Out-of-range profiles lose qualification and offer remembered unverified continuation; structural/signing changes, known failures and unresolved overlapping profiles hold before effects. [feat: "Text delivery"]
- [ ] Add `Tests/DeliveryPlanTests` and the `delivery-plans` suite for secure fields, unavailable web identity, version continuation, revocation, unsupported paragraphs, large content and competing profiles.
- [ ] [AUTO:run_cli] Run delivery-plan acceptance. Expect zero effects for unsupported content or unresolved targets, persistent preferences after restart, and no automatic requeue when focus or permissions return. [accept: command-exit: python3 scripts/acceptance.py delivery-plans]

## Phase phase_011: Execute delivery with conservative recovery

- [ ] Implement `DeliveryExecutor` as the sole owner of insertion and app clipboard mutations. Select the lowest queue sequence among eligible attempts while leaving held attempts visible. [feat: "Text delivery"]
- [ ] Persist operation intent before invocation and returned evidence afterward. Qualify synchronous AX success only within an evaluated settable-selected-text profile; treat messaging failures and posted Unicode events as uncertain. [feat: "Text delivery"]
- [ ] Implement insertion/submission states and per-operation target revalidation. Stop remaining effects on cancellation, revocation, failure or target change, preserving diagnostic operation ranges without inferring consumed text. [feat: "Text delivery"]
- [ ] Implement submission only after qualifying confirmed insertion and separate target/expected-selection checks. Journal and dispatch the selected sequence once; leave submission unconfirmed without its own acknowledgment. [feat: "Text delivery"]
- [ ] Implement clipboard snapshots of eager data up to 10 MiB, pre-write change-count checks, durable paste intent/barrier and recorded written count. Unsnapshottable contents require explicit replacement without restoration. [feat: "Text delivery"]
- [ ] Implement barrier eligibility and retirement under D-010. Block paste, Copy and restoration while allowing eligible AX/Unicode work. Timeouts, new captures and external clipboard changes cannot retire an unresolved barrier. [feat: "Text delivery"]
- [ ] Implement restart without replay, ownership-checked restoration after retirement and loss of memory-only snapshots. A missing written count prevents automatic restoration. Deletion retains text-free barrier metadata and discloses possible remaining clipboard text. [feat: "Text delivery"]
- [ ] Implement informed inspection, dismissal and edited-remainder recovery as new derived results/attempts. Require explicit duplicate-risk acknowledgment before repeating full text after uncertainty; unacknowledged insertion requires inspection before submission. [feat: "Text delivery"]
- [ ] Add the `delivery-recovery` suite with delayed paste consumers, equal-length selections, independent edits, focus changes after each operation, crash boundaries and all barrier retirement events.
- [ ] [AUTO:run_cli] Run delivery recovery acceptance. Expect no automatic duplicate attempt, unsafe restoration or submission after uncertainty; independent eligible delivery must continue around a paste barrier. [accept: command-exit: python3 scripts/acceptance.py delivery-recovery]
- [ ] Create `scripts/f4_trials.py` and frozen trial manifests for TextEdit, Mail, Notes, Safari, Chrome, Xcode, Terminal, VS Code, Slack, Messages, Figma and one preselected installed application. Record route, versions, settings, input methods, errors and interaction counts.
- [ ] [AUTO:run_cli] Run the automated F4 fixtures before profile qualification. Expect selected-text replacement within declared scope, no contradictory AX success, no raw-control leakage and no submission from verified paragraph mappings. Missing real-target evidence leaves profiles unevaluated. [accept: command-exit: python3 scripts/acceptance.py delivery-f4-fixtures]

## Phase phase_012: Implement cleanup, generation and translation

- [ ] Connect deterministic cleanup after committed recognition and before language processing. Publish cleanup as a derived result, then apply designated output replacements once after generation. Cancellation during refining preserves source recognition. [feat: "Vocabulary"]
- [ ] Implement bounded generation units with explicit context/KV/output limits. Faithful rewriting owns source spans and uses neighboring spans only as context; stage accumulated text on disk. [feat: "Custom modes"]
- [ ] Implement Standard without unintended language-model rewriting, and Email/Note generation under their permitted transformations. Retain dictated questions and prevent prompts from authorizing invented recipients, commitments or answers. [feat: "Built-in modes"]
- [ ] Implement translation to English through GenerationService from the retained source-language transcript after cleanup. A later rewrite names the translation as its parent and uses the separate generation charge. [feat: "Languages"]
- [ ] Implement `CheckReport` with revision, applicability, finding class and spans. Add structural schema/format/field checks and the specified English question-loss and answer-cue suspected-semantic checks. [feat: "Built-in modes"]
- [ ] Implement advisory quantity, negation, terminology, expansion and context-copy checks with their documented limits. Missing checks follow the frozen allow-with-notice or review-before-delivery policy without preventing model execution. [feat: "Custom modes"]
- [ ] Implement complete-candidate publication with held-for-review disposition for structural or suspected-semantic violations. Preserve incomplete output as hidden staging, and require explicit review or derived edits to release held candidates. [feat: "Built-in modes"]
- [ ] Integrate generation failures, truncation and cancellation with retained inputs. Hold truncated units or require a new bounded configuration/run; never silently truncate a recording or overwrite its source transcript. [feat: "Languages"]
- [ ] Add the `generation` suite covering instruction-like questions, malicious context, unavailable checks, valid empty output, boundary spans, translation dependencies and every specified candidate disposition.
- [ ] [AUTO:run_cli] Run generation acceptance. Expect preserved source results, correct dependency identities, specified holds/notices and no delivery of incomplete output. Record semantic fidelity as pending human evaluation. [accept: command-exit: python3 scripts/acceptance.py generation]

## Phase phase_013: Implement bounded diarization and checkpoints

- [ ] Implement per-stream 16 kHz mono analysis conversion with 600-second windows and 570-second steps, scheduled sequentially under one diarization charge. Clip local intervals at overlap midpoints into half-open ownership intervals. [feat: "Meetings"]
- [ ] Implement `DiarizationCoordinator` outside the UI thread. Validate execution epochs and copy one service window into run-owned staging before applying clipping and proposal processing. [feat: "Meetings"]
- [ ] Implement durable cluster evidence and a 256-entry centroid cache with paged prior-evidence scans. Keep growing cluster/output storage on disk without a speaker-count quota. [feat: "Meetings"]
- [ ] Implement up to three centroid proposals at distance at most 0.35, ties within 0.02, and adjacent-overlap proposals requiring two seconds plus intersection/union of at least 0.6. Label thresholds as uncalibrated and prohibit automatic cross-stream identity. [feat: "Meetings"]
- [ ] Implement synchronized per-window payloads and checksummed checkpoints containing stream/input identity, window index, runtime revision, parameters and artifact lengths. Acknowledge service output only after app checkpoint commit. [feat: "Meetings"]
- [ ] Implement complete-stream coverage validation, hidden preparation and one final aggregate publication. Unfinished window data never appears as completed diarization. [feat: "Meetings"]
- [ ] Implement interrupted-resumable recovery from valid checkpoint prefixes. Repeated launches must preserve the held state without inference admission, execution leases or automatic model loading. [feat: "Meetings"]
- [ ] Implement explicit Resume validation, queued continuation and admission-time revalidation with a new epoch. Missing assets remain explained and held; changed inputs/settings create a new run, and cancellation removes staging after leases drain. [feat: "Meetings"]
- [ ] Add the `diarization-recovery` suite for interrupted handoff, invalid tails, zero checkpoints, held/queued deletion, missing models, repeated interruption and service scratch cleanup.
- [ ] [AUTO:run_cli] Run diarization recovery acceptance. Expect continuation at the next committed stream/window, preserved app checkpoints after service cleanup, no partial publication and no launch-time inference for interrupted-resumable work. [accept: command-exit: python3 scripts/acceptance.py diarization-recovery]

## Phase phase_014: Integrate meeting capture, speaker editing and notes

- [ ] Add dedicated Start Meeting setup with remembered source presets and an explicitly selected notes mode. Default to microphone plus system audio when available and require acknowledgment of a missing requested source before starting. [feat: "Meetings"]
- [ ] Expose All system audio or a selected application's audio, recording the scope in the seed. Explain that All system audio includes other playback and never broaden an unavailable selected-application scope silently. [feat: "Input sources"]
- [ ] Implement one live-session arbitration with separate Stop Meeting and Discard recording controls. Reject dictation/per-mode/push-to-talk triggers during meetings without changing their audio or mode, and reject Start Meeting during any live session. [feat: "Recording controls"]
- [ ] Connect sequential bounded preview of selected streams, stream-specific waveform/status and recent visible transcript tails. Stop seals once and initiates configured analysis without automatic external delivery. [feat: "Recording display"]
- [ ] Implement native-frame/host-time fitting, gaps and common-timeline mapping. Use host-aligned-unverified status only with valid timestamps and maximum residual at most 50 ms; preserve simultaneous speech and degraded alignment. [feat: "Meetings"]
- [ ] Merge independently recognized streams with stable timing/IDs and associate text only with intersecting same-stream speaker evidence. Retain ambiguous candidates and echo provenance without automatic deduplication. [feat: "Meetings"]
- [ ] Implement immutable speaker revisions for names, assignments, splits, merges and batch confirmation. Only direct action on the current revision creates assigned-by-user status; re-diarization carries names as unverified proposals. [feat: "Meetings"]
- [ ] Implement bounded cited-item generation and fan-in reduction for timestamped notes. Pin the merged transcript, aggregate diarization and speaker revision; use confirmed names only for user-assigned attribution and retain unsupported sections as empty. [feat: "Meetings"]
- [ ] Add the `meetings` suite for every source combination, shortcut conflicts, missing streams, explicit discard despite keep-interrupted preference, speaker edit provenance, citation ranges and name/split drift.
- [ ] [AUTO:run_cli] Run meeting integration acceptance. Expect preserved meeting ownership under conflicting controls, immutable historical names/citations, visible ambiguous attribution, no automatic notes delivery and no resurrection after discard/publication races. [accept: command-exit: python3 scripts/acceptance.py meetings]

## Phase phase_015: Complete History and audio storage management

- [ ] Build paged recording/result-chain views with source, cleanup, translation, rewrite, manual-edit and notes dependencies. Changing the displayed result must affect presentation only. [feat: "History"]
- [ ] Implement FTS5 over approximately 16 KiB text chunks with streamed phrase verification across boundaries. Keep queries/caches bounded and exclude retained context from search. [feat: "History"]
- [ ] Implement seekable playback through immutable representation leases, preserving native frame counts and discontinuities. Rebuild missing seek caches from manifests and report unsupported codec revisions explicitly. [feat: "History"]
- [ ] Add reprocessing through selected models/modes with fresh immutable manifests. Missing original context requires an explicit omit-or-resample choice; missing assets/runtime revisions require replacement choices in a new run. [feat: "History"]
- [ ] Add manual and optional age-based retention using existing tombstones and leases. Support separate copied-source, audio and context removal, excluding new dependent jobs while allowing current holders to drain. [feat: "History"]
- [ ] Implement optional Optimize storage through DecodeService using per-chunk LZFSE, retaining PCM whenever compression expands it. Preserve decoded hashes, gain metadata and logical audio identity; leave optimization disabled by default. [feat: "History"]
- [ ] Implement ready/active/superseded representation transitions under expected-pointer and generation checks. Protect replacement staging with creators/claims, reserve conservative temporary space, and let existing leases keep old manifests usable. [feat: "History"]
- [ ] Implement post-switch retirement through ArtifactBroker. Shared chunks remain reachable through the new representation; unshared superseded chunks become reclaimable after leases drain, independent of historical diagnostic rows. [feat: "History"]
- [ ] Add `scripts/audio_storage_trials.py` for exact decode, mixed encodings, thousands of chunks, pointer interruption, low space, concurrent playback and optional LZFSE-versus-ALAC/decode-on-demand experiments. Keep experimental alternatives outside the production format reader until accepted.
- [ ] [AUTO:run_cli] Run History/storage acceptance. Expect phrase matches across chunks, exact post-gain playback through replacement, active PCM preserved on failed optimization, and safe reclamation without superseded files or membership caches. [accept: command-exit: python3 scripts/acceptance.py history-storage]
- [ ] [AUTO:run_cli] Run import/storage comparisons. Record exact samples, seeking, CPU, temporary space and storage with no assumed compression savings. Missing comparative evidence must leave current PCM/import defaults unchanged. [accept: command-exit: python3 scripts/acceptance.py storage-comparisons]

## Phase phase_016: Complete desktop behavior and cross-stage recovery

- [ ] Complete menu bar access, application-local gain controls, pending/effective gain indication and permanent-clipping disclosure. Keep hardware input volume unchanged and expose recording, processing and recovery actions consistently. [feat: "Desktop behavior"]
- [ ] Implement permission capability checks at setup and before affected operations. Preserve imports under microphone denial, hold AX delivery on revocation, suppress secure-field context/delivery and explain safe-boundary recovery actions. [feat: "Recording controls"]
- [ ] Implement per-stream permission-loss behavior. A microphone meeting may continue after system-audio loss with a marker; system-only capture stops with its prefix, and loss of every selected stream seals an interrupted session. [feat: "Input sources"]
- [ ] Implement identified-player capability interfaces and validate player-specific pause/manual-resume controls before listing integrations. Automatic resume requires confirmed pause plus atomic conditional resume using a token invalidated by every intervening control action. [feat: "Desktop behavior"]
- [ ] Keep automatic restoration unavailable for real players without the required evidence. Reject global media-key/device-idle ownership substitutes, invalidate ownership after permission loss and retain explicit user resumption where validated. [feat: "Desktop behavior"]
- [ ] Complete startup reconciliation in authoritative order across deletion records, database publication, sessions, imports, runs, model staging, materializations, representations and delivery barriers. Recovery must remain repeatable when interrupted again. [feat: "History"]
- [ ] Add the `desktop-recovery` suite for every cancellation state, writer/worker failures, permission restoration, source disappearance, display changes and fake player actions that return to the original position.
- [ ] [AUTO:run_cli] Run desktop recovery acceptance. Expect stage-specific retained inputs, durable cancellation acknowledgment, no automatic delivery after recovery and no resume after intervening player control. [accept: command-exit: python3 scripts/acceptance.py desktop-recovery]
- [ ] [AUTO:run_cli] Run packaged permission/shortcut fixtures, including Escape in transient controls and focus changes between key-down/key-up. Expect unavailable bindings to be visibly disabled, unmatched gestures to reach the foreground app and menu cancellation to remain available. [accept: command-exit: python3 scripts/acceptance.py platform-permissions]

## Phase phase_017: Evaluate defaults, practical delivery and long inputs

- [ ] Create frozen F1/F5 dataset manifests and `scripts/evaluate.py`, recording consent, hardware/OS, asset hashes, runtime revisions and parameters. Preserve all accepted thresholds and distinguish unperformed, inconclusive, failed and accepted evidence.
- [ ] Implement the whisper.cpp comparison harness and equivalent-checkpoint mapping, recording conversion differences. Freeze twenty recordings per English/Spanish language set covering the specified conditions, with a separate code-switching set. [feat: "Languages"]
- [ ] Add F1 scoring for WER, meaning-changing errors and correction actions. Require corpus WER at most 12%, comparator degradation within 15% relative or one percentage point when comparator WER is below 5%, and at most two correction actions per hundred reference words. [feat: "Models"]
- [ ] Add latency and preview trials for cold/warm launch, eviction, linger, concurrent generation and preemption. Measure external-marker trigger latency, preview updates and final p95, including loading/unloading and preview-enabled/disabled recognition comparisons. [feat: "Recording display"]
- [ ] Add mixed-model fairness trials with at least twenty thirty-second dictations once per minute during queued two-hour meeting recognition. Include shared models and distinct larger-A/smaller-B models, quota-boundary arrivals and oldest-run cancellation. [feat: "Recording controls"]
- [ ] [USER] Have two reviewers score forty frozen examples per built-in mode and evaluated language on the accepted 0–2 rubric and adjudicate disagreements. Bilingual reviewers assess translation against retained source transcripts; review cited support, question preservation and malicious context separately from recognition errors. [feat: "Built-in modes"]
- [ ] [USER] Complete F4 practical sessions with at least fifty dictations per route/target, including twenty back-to-back. Choose each route before its attempt and review actual text, selection and submission effects. Include remembered unprofiled routes, version continuation, restart, revocation and separate single-line/multiline results. [feat: "Text delivery"]
- [ ] Add F5 meeting scoring for overlap, returning speakers, silence and echo. Measure each speaker-time error component, proposal precision/coverage, boundary error, correction time and confirmations per speaker-hour. Compare windowed and whole-input analysis without claiming bounded whole-input behavior from timings alone. [feat: "Meetings"]
- [ ] [USER] Review labeled meeting outputs and speaker proposals, recording initial naming, batch confirmations and all correction time. Judge citation support and uncertain identities. Preserve the accepted limits of five correction minutes per meeting hour and two confirmations per speaker-hour. [feat: "Meetings"]
- [ ] Implement resource instrumentation for one-second process/accelerator samples, stage peaks and settled five-minute checkpoints after ten minutes of warm-up. Use ten-minute, one-hour, three-hour and twelve-hour repeated-content inputs, plus allocation inspection for accumulating structures.
- [ ] Implement the accepted pilot/confirmation analysis. Use three pilots, freeze 8/16/24 confirmation runs and tolerance M from twice estimated slope error, rounded up within 1–4 MiB/hour. Inconclusive precision or any identified duration-dependent accumulation blocks resource acceptance.
- [ ] [AUTO:run_cli] Run F1/F4/F5 scoring over completed evidence. Expect preview within two seconds and subsequent updates within three, final p95 within six seconds, trigger p95 within 300 ms, preview WER upper bound below one percentage point, F4 resolution actions at most 0.1/dictation, and the accepted semantic-review limits. Missing observations must fail evidence completeness. [accept: command-exit: python3 scripts/acceptance.py evaluated-defaults]
- [ ] [AUTO:run_cli] Run meeting/resource acceptance. Expect speaker-time error at most 15%, non-tied proposal precision at least 95%, timestamp disagreement at most 50 ms over sixty minutes, effort limits met, footprints within admission ceilings and the one-sided 95% growth bound below frozen M with no established positive growth. [accept: command-exit: python3 scripts/acceptance.py meetings-resources]
- [ ] [USER] Review failed or inconclusive criteria with measured traces and concrete adapter/configuration or behavior proposals. Approve consequential changes before altering criteria or advertising support. Preserve every required capability and keep compatible imported-model use independent of default-quality acceptance.

## Phase phase_018: Verify the complete application and prepare direct releases

- [ ] Add the `application-lifecycle` suite covering capture through delivery, imports through reprocessing, meetings through notes, concurrent deletion, model replacement, representation retirement and repeated restart. Use the production composition root and signed bundle.
- [ ] Add a routine-offline suite covering every inference stage, retained context, local imports, playback and reprocessing with networking unavailable. Verify that only explicit model acquisition can start the downloader and that no recording/context handles reach it.
- [ ] Add migration and upgrade fixtures for immutable manifests, historical runtime descriptors, archived models, settings revisions and unresolved paste barriers. Preserve user assets and prevent old workers from retaining authority after replacement.
- [ ] [AUTO:run_cli] Run full application acceptance. Expect all stage-specific recovery contracts, no deleted-record resurrection, no uncertain-effect replay, immutable source/result relationships and bounded preparation during concurrent use. [accept: command-exit: python3 scripts/acceptance.py application-lifecycle]
- [ ] [AUTO:run_cli] Run offline acceptance. Expect all installed local workflows to operate without network access, missing models to produce local recovery choices and no implicit download or HTTP inference fallback. [accept: command-exit: python3 scripts/acceptance.py routine-offline]
- [ ] Complete Developer ID packaging with hardened runtime, service-first signing, notarization, stapling and signed user-initiated update packages. Include exact dependency/model notices and retain the development bundle structure.
- [ ] [AUTO:run_cli] Run release packaging and F2 against Developer ID builds on the supported macOS baseline. Expect valid nested signatures/notarization, enforced worker isolation, successful local model loading and preserved permission identity. Missing credentials or hardware must remain an unmet release prerequisite. [accept: command-exit: python3 scripts/acceptance.py release-package --signing developer-id]
- [ ] Document measured model/language coverage, target-profile scope, microphone startup behavior, storage/clipping consequences, clipboard uncertainty and recovery actions. State the initial arm64 engineering limitation and the complete-stack evidence required before advertising Intel support.
- [ ] [USER] Have the user or named product delegate review practical dictation, transformation fidelity, meeting correction burden and release claims against the accepted criteria. Record design approval, integration evidence and release acceptance separately, with unresolved required-capability failures blocking the affected release.