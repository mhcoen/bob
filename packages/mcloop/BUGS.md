## Bugs

- [ ] Fix `[USER]` task body loss at parse time, not merely rendering: in `mcloop/mcloop/checklist.py`, `parse()` only emits `Task`s for `CHECKBOX_RE` lines, non-checkbox lines hit `if not m: continue`, and `text = m.group(3).strip()` captures only the checkbox line, so a multi-line `[USER]` body is lost before any rendering; `user_task_instructions` currently returns first-line-only and `formatting.user_banner` never receives the body. The old BUGS.md wording that this "renders as a blob" was wrong: it is parse-time loss, not a render bug. Fix direction: retain contiguous non-checkbox lines following a `[USER]` checkbox until the next checkbox or heading on the `Task` (for example a new `body` field), surface that via `user_task_instructions`, and render it line-structured in `user_banner`, with command-looking lines isolated by blank lines and no reflow. Required tests: a multi-line `[USER]` body round-trips parse -> `user_task_instructions` -> `user_banner` with newlines intact. mcloop must stay green under its configured checks.
- [ ] Preserve failed `[USER]` observations when filing BUGS.md entries: in `mcloop/mcloop/main.py` around the failed `[USER]` branch, the current code uses `response.replace("\n", " | ")`, `short_obs = flat_obs[:200]`, appends `...`, and files `bug_desc`, which flattens and truncates the user's observation. Fix direction: stop replacing newlines with ` | ` and stop the 200-character cut; preserve the observation in the filed BUGS.md entry as fenced text. Required tests: a long multi-line failed `[USER]` observation files to BUGS.md untruncated and unflattened. mcloop must stay green under its configured checks.
- [ ] Fix check-command executable resolution for target-project venvs: observed against `/Users/mhcoen/proj/bob-tools` this session, mcloop bug-only mode applied a valid `bob_tools/planfile/parser.py` fix, then crashed in post-edit checks before check-off/commit with `FileNotFoundError: [Errno 2] No such file or directory: 'mypy'` because it invoked bare `mypy` from PATH instead of the target project's `.venv/bin/mypy`; the failure window is after editing and before verify/commit, leaving a verified-green fix stranded uncommitted and forcing an out-of-band commit. Investigate from source where check executables are resolved, especially `mcloop/mcloop/checks.py`, `mcloop/mcloop/test_runner.py`, and `mcloop/mcloop/checklist.py`; have check detection prefer a detected target project venv (`.venv/bin`) before PATH for ruff, pytest, and mypy rather than normalizing or appending bare tool names. Required tests: a project with only `.venv/bin/{ruff,pytest,mypy}` and no PATH tools runs bug-only checks without `FileNotFoundError`; mcloop must stay green under its configured checks.
