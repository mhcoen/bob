## Bugs

- [x] T-000001: Fix `[USER]` task body loss at parse time, not merely rendering: in `mcloop/mcloop/checklist.py`, `parse()` only emits `Task`s for `CHECKBOX_RE` lines, non-checkbox lines hit `if not m: continue`, and `text = m.group(3).strip()` captures only the checkbox line, so a multi-line `[USER]` body is lost before any rendering; `user_task_instructions` currently returns first-line-only and `formatting.user_banner` never receives the body. The old BUGS.md wording that this "renders as a blob" was wrong: it is parse-time loss, not a render bug. Fix direction: retain contiguous non-checkbox lines following a `[USER]` checkbox until the next checkbox or heading on the `Task` (for example a new `body` field), surface that via `user_task_instructions`, and render it line-structured in `user_banner`, with command-looking lines isolated by blank lines and no reflow. Required tests: a multi-line `[USER]` body round-trips parse -> `user_task_instructions` -> `user_banner` with newlines intact. mcloop must stay green under its configured checks.
- [x] T-000002: Preserve failed `[USER]` observations when filing BUGS.md entries: in `mcloop/mcloop/main.py` around the failed `[USER]` branch, the current code uses `response.replace("\n", " | ")`, `short_obs = flat_obs[:200]`, appends `...`, and files `bug_desc`, which flattens and truncates the user's observation. Fix direction: stop replacing newlines with ` | ` and stop the 200-character cut; preserve the observation in the filed BUGS.md entry as fenced text. Required tests: a long multi-line failed `[USER]` observation files to BUGS.md untruncated and unflattened. mcloop must stay green under its configured checks.
- [x] T-000003: Fix check-command executable resolution for target-project venvs: observed against `/Users/mhcoen/proj/bob-tools` this session, mcloop bug-only mode applied a valid `bob_tools/planfile/parser.py` fix, then crashed in post-edit checks before check-off/commit with `FileNotFoundError: [Errno 2] No such file or directory: 'mypy'` because it invoked bare `mypy` from PATH instead of the target project's `.venv/bin/mypy`; the failure window is after editing and before verify/commit, leaving a verified-green fix stranded uncommitted and forcing an out-of-band commit. Investigate from source where check executables are resolved, especially `mcloop/mcloop/checks.py`, `mcloop/mcloop/test_runner.py`, and `mcloop/mcloop/checklist.py`; have check detection prefer a detected target project venv (`.venv/bin`) before PATH for ruff, pytest, and mypy rather than normalizing or appending bare tool names. Required tests: a project with only `.venv/bin/{ruff,pytest,mypy}` and no PATH tools runs bug-only checks without `FileNotFoundError`; mcloop must stay green under its configured checks.
- [ ] T-000004: Preserve resolved bug history when purging the live BUGS.md queue: now that `BUGS.md` is git-tracked, bug-only mode still calls `purge_completed_bugs(bugs_path)` in `mcloop/mcloop/main.py`, and `mcloop/mcloop/checklist.py` deletes checked-off bug lines and rewrites `BUGS.md`; this keeps revert-consistency for open bugs, but resolved bugs are erased from the readable ledger before they are committed in checked-off form and are only reconstructable through git diff archaeology. Fix direction: change `purge_completed_bugs` from delete-checked-off-lines to move checked-off entries into a separate git-tracked append-only history file named `BUGS-resolved.md` (confirmed no current file and no `.gitignore` match). Requirements: `BUGS.md` remains a lean live queue parsed by the loop, so purge must still remove resolved entries from `BUGS.md` and hot-path token cost does not grow; resolved entries are appended verbatim to `BUGS-resolved.md`; `BUGS-resolved.md` is never parsed by the bug-only loop; `BUGS-resolved.md` is tracked in git for durability; existing tests `tests/test_checklist.py::test_purge_completed_bugs_basic` and `tests/test_checklist.py::test_purge_completed_bugs_all_done` currently encode delete-only behavior and must be updated as part of the fix, not treated as regressions; mcloop stays green under its configured checks.
- [x] T-000005: Completion integrity defect A, accepted no evidence: vacuous-green already-satisfied checkoff can mark implementation work complete without objective task/stage-specific acceptance evidence. Source: batch no-change path in `mcloop/mcloop/main.py:458-468` runs `run_checks(project_dir)` and checks off all batch children if globally green; single-task no-change path in `mcloop/mcloop/main.py:1503-1527` does the same for one task; phase boundary in `mcloop/mcloop/main.py:1097-1151` runs only the configured full suite before transitioning; `mcloop/mcloop/checks.py:230-249` has no stage-specific acceptance-evidence requirement. Concrete manifestation: bob-tools Stage 6 was checked off even though `bob_tools/planfile/fileio.py:update()` remained a `NotImplementedError` stub and zero Stage-6 falsifying tests for crash-safety, advisory locking, concurrent update serialization, or mid-flight external-edit detection existed or were collected. Distinct from defect B: this accepted no evidence. Fix direction: no-op/already-satisfied checkoff and phase transition must require objective stage/task-specific acceptance evidence: the stage's newly-required tests or AUTO/manual verification artifact must demonstrably exist and have executed. If an implementation stage has no falsifying evidence, mcloop must refuse to check it off or file a bug rather than treating global green as sufficient. Required regression tests: batch no-op false positive and single-task no-op false positive must fail to check off without executed acceptance evidence; mcloop must stay green under its configured checks.
- [x] T-000006: Completion integrity defect B, ignored explicit failing evidence: nonzero AUTO/verification results are recorded as observations but do not fail the owning task or halt the run. Source: AUTO tasks in `mcloop/mcloop/main.py:1181-1191` call `_handle_auto_task`, then unconditionally `check_off(active_file, task)`, append completion, and continue; `_handle_auto_task` in `mcloop/investigate_cmd.py:459-491` returns a text observation only; `run_cli` dispatch in `mcloop/investigate_cmd.py:501-509` records `exit_code: <n>` and `STATUS: CRASHED` when `exit_code != 0`, but that status is not propagated to task failure. Concrete manifestation: bob-tools tasks 7.3 and 8.4 ran AUTO `run_cli` verifications that exited 1 with `STATUS: CRASHED` (`bob-plan not found`), mcloop recorded the observations and still continued to `All tasks completed`. Distinct from defect A: this ignored explicit failing evidence. Fix direction: a nonzero `exit_code`, hung result, or `STATUS: CRASHED` from an AUTO/verification action must fail the owning task and enter the existing retry/halt/escalation path; it must never be silently recorded and passed. Required regression test: a nonzero AUTO `run_cli` result fails the task, leaves it unchecked, and does not reach run completion; mcloop must stay green under its configured checks.
- [x] T-000007: Defect C, substring `[USER]` misclassification blocks bug-only mode on `[USER]`-describing entries: `mcloop/mcloop/checklist.py:627-633` currently classifies a task as user-observation work when the literal substring `[USER]` appears anywhere in `task.text`. Concrete manifestation: bug-only mode halted at the existing BUGS.md entry whose description merely discusses `[USER]` rendering, entered `user_prompt`, and never reached completion-integrity defects A and B; `.mcloop/interrupted.json` showed `task_label` 1 and `phase` `user_prompt`. Distinct from A and B: this is not accepted no evidence or ignored failing evidence, but misclassification of descriptive prose as an operational marker. Fix direction: user-task detection must distinguish a `[USER]` marker at the task-text position (a leading tag/token immediately after the checkbox) from a literal `[USER]` substring in prose; anchor/tokenize the match instead of substring-searching. Required regression test: a BUGS.md/PLAN.md entry whose description contains `[USER]` in prose is not classified as a user task, while a genuine leading `- [ ] [USER] ...` marker task is still classified as a user task. mcloop must stay green under its configured checks.
