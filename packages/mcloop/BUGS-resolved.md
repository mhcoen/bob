## Resolved Bugs

- [x] T-000001: In `mcloop/coverage_verify.py` (and `change_class.py` as needed), define a no-test-needed change class for non-code inputs (dependency manifests like pyproject.toml, tool config such as ruff/mypy/pytest, requirements/lock files, and plain data files) and make the coverage gate pass such changes without a mapped test and without forcing a waiver. Do NOT hardcode a single-file `pyproject.toml` whitelist; generalize the class. Keep the existing test/coverage requirement for executable `.py` source. [fix: "gate exempts non-code changes that need no test"] <!-- completed_at: 2026-06-05T01:36:37Z -->
- [x] T-000002: Add tests: a `pyproject.toml` dependency edit (and a ruff/mypy config edit, and a data-file edit) passes the gate with no mapped test and no waiver; a behavioral `.py` change with no exercising test still fails the gate. [fix: "regression: non-code exempt, code still gated"] <!-- completed_at: 2026-06-05T01:39:38Z -->
- [x] T-000003: In `mcloop/waivers.py:has_waiver`, stop requiring exact `baseline_sha` equality: match a recorded waiver on changed input plus task identity so a commit/baseline change does not silently nullify a user-recorded waiver (or detect the stale SHA and re-prompt instead of ignoring). [fix: "recorded waiver survives baseline change"] <!-- completed_at: 2026-06-05T01:42:49Z -->
- [x] T-000004: Provide a way to retry a `[!]`-failed task once its blocking condition is cleared (reset-to-pending path or equivalent), instead of permanently skipping it. [fix: "cleared failed task can be retried"] <!-- completed_at: 2026-06-05T02:05:00Z -->
- [x] T-000005: Add tests: a recorded waiver still matches after an intervening commit changes the baseline SHA; a previously-failed task becomes runnable again after reset. [fix: "regression: waiver match post-commit and failed-task retry"] <!-- completed_at: 2026-06-05T02:08:27Z -->
- [x] T-000001: Fix `mcloop`'s `parse_auto_task` so that an automated run-cli task (action `run_cli`) executes only the backtick-quoted command extracted from the task text, not the full prose description. If the task text contains a single backtick-delimited command, run exactly that; if it contains none, return a clear failure rather than passing prose to the shell. Do not change parsing for other automated actions. [fix: "run_cli extracts backtick-quoted command from task prose"] <!-- completed_at: 2026-06-05T02:55:25Z -->
- [x] T-000002: Add tests: an automated run-cli task of the form "Run `<cmd>` to confirm ..." executes exactly `<cmd>` (assert the prose words are not passed to the shell); a run_cli task with no backtick command yields a clear failure, not a 127 from shell-parsing prose. [fix: "regression: run_cli command extraction"] <!-- completed_at: 2026-06-05T02:58:43Z -->
- [x] T-000001: In `mcloop/coverage_verify.py` (using an AST check, consistent with `change_class.py`), add detection of a coverage-exempt Python file class: a `.py` file whose classes are all `typing.Protocol` subclasses or ABCs with only abstract/`...`/`pass`/docstring-only method bodies, and/or whose module body is solely imports and re-exports with no executable statements. When a changed `.py` file is in this class, the coverage gate passes it without requiring an executed-line/mapped-test proof and without forcing a waiver. Files containing any executable logic remain gated as today. [fix: "coverage gate exempts Protocol/ABC/re-export-only Python files"] <!-- completed_at: 2026-06-05T04:39:05Z -->
- [x] T-000002: Add tests: a Protocol-only module, an ABC-only module, and an import-and-re-export-only `__init__.py` each pass the gate with no mapped test and no waiver; a module with real executable logic and no exercising test still fails the gate; a module mixing a Protocol with one real executable function is NOT exempt (still gated). [fix: "regression: un-coverable Python exempt, real logic still gated"] <!-- completed_at: 2026-06-05T04:40:30Z -->

### `parse_auto_task` now over-rejects: a bare-path/bare-command run_cli task with no backticks errors instead of running

**Symptom**: building writer, the phase-1 verify task `[AUTO:run_cli] /Users/mhcoen/proj/writer/scripts/verify_phase1.sh` fails with `ERROR: run_cli task has no backtick-delimited command`. The task text is a bare absolute path with no surrounding prose and no backticks. This is a regression from the earlier fix that made `parse_auto_task` extract the backtick-delimited command from prose (which fixed the "Run `<cmd>` to confirm ..." case): that fix made backticks mandatory, so a legitimate bare-path/bare-command AUTO task now errors. Every phase's verify task phrased as a bare path hits this.

**Root cause**: the corrected `parse_auto_task` requires a backtick-delimited span and raises when none is present. But an AUTO run_cli task's args can legitimately be (a) prose containing a backtick-quoted command ("Run `<cmd>` to confirm ..."), or (b) a bare command/path that is itself the whole args with no prose and no backticks. The fix handled (a) but broke (b).

**Chosen fix**: `parse_auto_task` for run_cli should: if the text contains a backtick-delimited command, run exactly that (case a); else if the entire args is a single bare command/path with no surrounding prose (no spaces beyond the command, or it resolves to an existing script path), run it as-is (case b); else (prose with no extractable command) error as it does now. Bare path/command and backtick-quoted command are both valid; only genuinely unextractable prose errors.

### Tasks

- [x] T-000003: Fix `mcloop`'s `parse_auto_task` so a run_cli task whose args is a bare command/path with no backticks and no surrounding prose runs as-is, while still extracting the backtick-delimited command when the args is prose containing one, and still erroring only when the args is prose with no extractable command. [fix: "run_cli accepts bare command as well as backtick-quoted command in prose"] <!-- completed_at: 2026-06-05T04:42:58Z -->
- [x] T-000004: Add tests covering all three run_cli arg shapes: (1) prose with a backtick-quoted command runs exactly that command; (2) a bare path/command with no backticks runs as-is; (3) prose with no extractable command errors with a clear message. [fix: "regression: run_cli handles bare, backtick-quoted, and unextractable args"] <!-- completed_at: 2026-06-05T04:44:52Z -->
- [x] T-000001: In `mcloop`'s AUTO run_cli handler, when the command to run is a path ending in `.sh`, invoke it as `bash <path>` (with any trailing arguments preserved) rather than executing the path directly, so a non-executable (mode 644) script runs successfully instead of failing with exit 126. Leave non-`.sh` commands invoked exactly as they are today. [fix: "AUTO run_cli invokes .sh scripts via bash, not direct execution"] <!-- completed_at: 2026-06-05T04:51:26Z -->
- [x] T-000002: Add a regression test for the AUTO run-cli handler: a run-cli task pointing at a mode-644 (non-executable) dot-sh script runs successfully (exit 0, not 126); and a non-dot-sh command is still invoked directly (not bash-wrapped). [fix: "regression: 644 sh script runs via bash; non-sh unchanged"] <!-- completed_at: 2026-06-05T05:01:18Z -->
- [x] T-000028: Batch sessions burn retry attempts on transient rate/session limits and never use chain fallover. `_run_batch` (main.py ~886) returns a plain "failed" on a limit hit, and the caller loop (main.py ~1800) consumes a `batch_attempt` and feeds the limit-noise output tail back as `batch_prior_errors`; the `fallback_model` param is ignored entirely. A few transient limits during a large batch exhaust `max_retries` and `mark_failed` the parent and all children even though nothing failed on the merits. The non-batch path already handles this correctly (`attempt -= 1` on a limit, main.py ~2060) -- mirror it: detect `is_rate_limited`/`is_session_limited` on the batch output, do not consume an attempt, and wait-for-reset/fall over via the same ratelimit machinery. Regression test: canned limit-text batch output does not decrement attempts and does not mark tasks failed. (Found in the 2026-07-02 cross-package audit; residual scope of T-000027 in the workspace root BUGS.md.) <!-- completed_at: 2026-07-03T07:23:20Z -->
- [x] T-000029: Migrate the remaining direct `subprocess.run(["git", ...])` call sites onto `git_ops.run_git_bounded` so every git invocation gets the timeout and `GIT_TERMINAL_PROMPT=0` guards. The 2026-07-02 audit hardened `git_ops._git` and `worktree._run_git`, but ~13 bare sites remain: errors.py ~195 (git log), investigate_cmd.py ~315/~326 (git log/diff), git_ops.py ~152/~167/~172 (init/add/commit inside git_ops itself, bypassing its own wrapper) and ~749 (git show), ledger_emit.py ~274-357 (six calls), reviewer.py ~480 (git diff), review_integration.py ~26 (git rev-parse). `git commit` runs hooks and shares exactly the hang risk the wrapper exists for. Mechanical change; add a test asserting no mcloop module invokes subprocess.run with a git argv outside git_ops. <!-- completed_at: 2026-07-03T07:32:01Z -->
- [x] T-000030: `_build_command` only applies `_apply_provider_env` when `env is not None` (runner.py ~873), and `_run_session` silently builds its own env when passed none -- so any new call site that forgets to thread one env through both calls runs unrouted on third-party models. This bug class has shipped at least twice (code_edit.py ~404 documents the first fix; the 2026-07-02 audit fixed five more sites, since funneled through `_run_claude_subsession`). Make it impossible rather than merely centralized: have `_build_command` own env construction and return `(cmd, env)` (or add a `_prepare_session` helper used by ALL call sites including code_edit.py), and remove the env-None-skips-routing branch. Note main.py ~504 (`_preflight_chain`) calls `_build_command` purely for its env side effect and discards the command -- evidence the env work is misplaced inside command building. Regression: no runner entry point can produce a session command without provider routing applied. <!-- completed_at: 2026-07-03T07:40:54Z -->
- [x] T-000031: Run-summary write failures are silently swallowed: main.py ~1074 wraps `write_run_summary` in `except Exception: return None` and callers ignore the result, so a disk/permission error leaves a stale `.mcloop/runs/latest.json` with no warning -- and anything gating on latest.json (the orchestra smoke-test procedure reads it) then validates against the previous run. Surface the failure to stdout and notify at error level, keeping the run alive. Regression test: a raising summary writer produces a visible warning and does not crash the loop. <!-- completed_at: 2026-07-03T07:45:34Z -->
- [x] T-000032: Unreachable graceful-interrupt path: main.py ~2028-2059 wraps `time.sleep(SESSION_LIMIT_POLL)` in try/except KeyboardInterrupt to write an "interrupted" run summary, but the installed SIGINT handler (lifecycle.py ~498-508) calls `os._exit(130)` unconditionally, so KeyboardInterrupt never reaches the main thread and the summary-write branch is dead code. Either remove the dead branch or make the lifecycle handler perform the summary write before exiting -- the mcloop CLAUDE.md smoke-test section already documents that latest.json is NOT updated on SIGINT, and fixing that would be the better outcome. Decide one way; do not leave the dead handler in place. <!-- completed_at: 2026-07-03T07:58:43Z -->
- [x] T-000033: Batch selective rollback parses `git diff --name-only` / `git ls-files --others` output with `.splitlines()` and feeds the paths to checkout/unlink/rmtree (main.py ~1013-1037): `core.quotepath` escaping of non-ASCII names produces quoted paths that match nothing, embedded newlines break the split, and `shutil.rmtree` on a symlink-to-directory raises -- so a failed batch may not fully restore the pre-batch tree before retry. Use `-z` NUL-delimited output for both commands and guard the symlink case. Regression test: rollback restores a file with a non-ASCII name. <!-- completed_at: 2026-07-03T08:18:14Z -->
- [x] T-000035: `PlanNotCanonicalError`'s remediation message tells the user to "Run: bob-plan migrate <path>", but bob-plan has no migrate subcommand (choices are validate/next/fmt/done/fail) -- observed 2026-07-03 while filing audit bugs. Either bob-plan grows the migrate command the message promises, or the message (mcloop/_planfile_precondition.py, PlanNotCanonicalError construction) should name the real command. Coordinate with the bob-tools entry about fmt refusing to assign ids on marker-bearing files: as of 2026-07-03 there is NO working tool path to canonicalize a marker-bearing file with bare checkboxes; ids had to be assigned by hand. Regression test: the remediation command named in the error actually exists and canonicalizes the file it was raised for. <!-- completed_at: 2026-07-03T08:38:10Z -->
- [x] T-000036: Installed hooks drift silently from the repo copies: ~/.claude/hooks/telegram-permission-hook.py dated 2026-05-29 (525 lines) was missing the entire McLoop test-routing policy that the repo copy (722 lines) ships, so on 2026-07-03 an editor session ran free-form pytest through a Telegram ask instead of the instant mcloop-verify deny, and the whole test-discipline gate was inactive without any signal. Nothing detects or reports the skew: `mcloop install` copies hooks once and no later run compares the installed copy against the repo version. Fix: version-stamp the hook files (or hash-compare) and have run_loop startup (or install_cmd) warn loudly -- or auto-reinstall -- when the installed hook differs from the packaged one. Regression test: a stale installed hook triggers the warning path. <!-- completed_at: 2026-07-03T08:44:53Z -->
- [x] T-000034: Formatting discipline varies by editor model: the codex/gpt-5.5 fallover tier ships unformatted code that Claude-family editors would have formatted, so a model swap mid-phase changes the artifact quality. Observed 2026-06-11 (writer, task 20.5): fable failed 3 attempts, the chain fell over kimi/deepseek (402) to codex/gpt-5.5, which completed the task with a test file that failed the phase boundary's `ruff format --check` -- the task-level acceptance (pytest) passed, so the defect surfaced only at the boundary, stopping the run a phase late and requiring a hand fix. Formatting must not depend on which model held the editor seat. Fix: run the project's formatter deterministically in the loop after every editor session and before the acceptance check (e.g. `ruff format` on the changed files when the project configures ruff), the same way commits are mcloop's job and not the model's -- format-on-exit is scaffolding work, not a model habit to hope for. Regression test: a stubbed editor session that writes a deliberately misformatted file still yields a task commit that passes `ruff format --check`. <!-- completed_at: 2026-07-03T09:03:35Z -->
