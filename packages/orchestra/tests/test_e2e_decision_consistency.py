"""Retired: F2.5a end-to-end coverage moved with the workflow.

The original e2e smoke for the decision-consistency invariant rode on
``iterate_until_acceptable``. That workflow has been retired (T-000016);
this file is intentionally empty pending design_loop-specific e2e
coverage. The pure-function invariant remains pinned in
``test_decision_consistency.py``.
"""

from __future__ import annotations
