{
  "criteria": [
    {"id": "exact_12_words",  "description": "Title is exactly 12 words.",                 "required": true},
    {"id": "contains_phrase", "description": "Title contains 'workflow orchestration'.",    "required": true},
    {"id": "ends_question",   "description": "Title ends with a question mark.",            "required": true}
  ],
  "roles": {
    "proposer":   { "adapter": "claude_code_text", "model": "haiku",   "parameters": {} },
    "reviewer":   { "adapter": "codex_text",       "model": "gpt-5.5", "parameters": {} },
    "judge_role": { "adapter": "codex_text",       "model": "gpt-5.5", "parameters": {} }
  },
  "workflows": {
    "iterate_until_acceptable": { "pattern": "iterate_until_acceptable" }
  }
}
