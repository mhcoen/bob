## Resolved Bugs

- [x] T-000001: In `duplo/workflows/templates/plan_author_judge.md`, inject the three configured criterion ids and their descriptions from `duplo/plan_author_role.py:PLAN_AUTHOR_CRITERIA` (`task_granularity_5_to_15`, `batch_user_auto_discipline`, `feat_fix_annotations_present`) into the judge prompt, and instruct the judge to emit `criteria_compliance` with exactly one entry per configured criterion using those exact ids and no others (do not invent ids from the `_PHASE_SYSTEM` prose rules), so `orchestra/executor/criteria.py:check_decision_consistency` no longer fails with `missing_ids`. Thread the ids/descriptions from the binding rather than hardcoding a second copy of the strings so the template and binding cannot drift. <!-- completed_at: 2026-06-04T18:48:11Z -->
- [x] T-000002: Add a regression test that a `plan_author` judge verdict reports exactly the three configured criterion ids, and that the loop survives an `iterate` verdict without raising `missing_ids`. Run `pytest`, `ruff check .`, `ruff format --check .`, and `mypy .` and keep them green. <!-- completed_at: 2026-06-04T18:53:05Z -->
- [x] T-000009: Fix `duplo/plan_sanity.py:_check_scope_coverage` so it does not raise false `scope_uncovered` when a scope item is built under a paraphrased feature name or decomposed across multiple finer features. Match scope items to features/tasks by a robust strategy (e.g. token-overlap / key-noun-phrase match, matching against the leading label before the first colon, and treating an umbrella item as covered when its constituent subcommand/feature names are each built) rather than verbatim/substring identity. Bias toward not-flagging: only report `scope_uncovered` when no plausible builder exists. [fix: "sanity gate scope matcher must handle paraphrase and decomposition"] <!-- completed_at: 2026-06-04T22:04:41Z -->
- [x] T-000010: Add regression tests for the matcher using the two real writer cases: a scope item built under a parenthetical-qualified feat name, and an umbrella CLI scope item decomposed into per-subcommand features; both must be recognized as covered (no `scope_uncovered`). Add a true-negative test: a scope item genuinely built by no task is still reported. [fix: "regression: paraphrase/decomposition covered, genuine gap still caught"] <!-- completed_at: 2026-06-04T22:06:47Z -->

- [x] T-000001: In `duplo/extractor.py`, after `extract_features` parses the LLM feature list, add deterministic reconciliation against `scope_include`: for every include item with no matching extracted feature (case-insensitive name match), synthesize and append a `Feature` record sourced from the spec item, so a user scope item present in no scraped Source is never dropped. User spec scope is authoritative; scraped Sources only add. [fix: "scope_include items must deterministically become features"] <!-- completed_at: 2026-06-04T19:53:29Z -->
- [x] T-000002: In `duplo/roadmap.py`, ensure every feature originating from a `scope_include` item is allocated to a build phase with real tasks (not left feature-only), so a guaranteed user scope item always reaches a buildable phase. [fix: "guaranteed scope features must reach a build phase"] <!-- completed_at: 2026-06-04T19:56:42Z -->
- [x] T-000003: Add a regression test asserting that a SPEC.md whose `## Scope` include list contains an item mentioned in no scraped Source still yields both a feature record and a phase that builds it. [fix: "regression: scope beyond sources is never dropped"] <!-- completed_at: 2026-06-04T19:59:36Z -->
- [x] T-000004: Locate the phase_id stamping site (`duplo/saver.py` and/or `duplo/reauthor_phase_ids.py`) and fix it so each saved phase receives a unique, sequential phase_id (phase_001..phase_NNN) tracking the phase position, instead of a constant `phase_001`. [fix: "phase_id must increment per phase"] <!-- completed_at: 2026-06-04T20:06:03Z -->
- [x] T-000005: Add a regression test asserting a generated/saved multi-phase plan has unique, sequential phase_id comments across all phases. [fix: "regression: phase_ids unique and sequential"] <!-- completed_at: 2026-06-04T20:10:21Z -->
- [x] T-000006: Add a whole-plan post-generation verifier (new module, e.g. `duplo/plan_sanity.py`) that, on the assembled PLAN.md, checks: every `## Scope` include item is built by >=1 phase; every behavior/verification task maps to a feature some phase builds; phase_ids are unique and sequential. [fix: "whole-plan sanity check exists"] <!-- completed_at: 2026-06-04T20:17:25Z -->
- [x] T-000007: Wire the verifier into the pipeline after full-plan assembly with the bounded loud-repair/hard-stop policy: repair known defect classes deterministically and log exactly what changed, re-validate exactly once, proceed if clean; on persistent or unknown-class failure, hard stop with an actionable report and do NOT retry. [fix: "bounded corrective gate, no retry loop"] <!-- completed_at: 2026-06-04T20:35:15Z -->
- [x] T-000008: Add a regression test for the gate: a known-bad plan (verify-without-build; duplicate phase_ids) triggers a logged repair and yields a clean plan in one pass; an unknown/unrepairable invariant violation produces a hard stop with a report and no retry. [fix: "regression: gate repairs known, stops on unknown, never loops"] <!-- completed_at: 2026-06-04T22:09:03Z -->
- [x] T-000001: In duplo's phase/roadmap generation, ensure that when a batch creates new executable modules, a covering test task that exercises those modules is emitted as a sibling task within the SAME batch (same parent), rather than deferring the covering test to a later phase. The batch must be self-contained so mcloop's coverage gate passes within it: created code and its exercising test are accepted together. [fix: "co-locate covering test task with module-creation tasks in the same batch"] <!-- completed_at: 2026-06-05T04:05:46Z -->
- [x] T-000002: Add a regression test asserting that for a generated plan, every batch that creates new `.py` modules also contains (as a sibling task in the same batch/parent) a test task that targets those modules, so no module-creation batch is left with its covering test deferred to a later phase. [fix: "regression: module batch includes its covering test"] <!-- completed_at: 2026-06-05T04:08:07Z -->
- [x] T-000001: `save_hashes` persists the seen-state too early in `_subsequent_run` (pipeline.py ~1707): site-media download (~1711), behavioral-video design/SPEC extraction (~1734/~1772), and gap detection (~1819) all run after it, so a crash in any of those permanently consumes the change trigger -- the next run diffs clean and the changed files are never re-analyzed until manually re-touched. Move the save after the last consumer of the diff (or save incrementally per completed stage). Regression test: a stubbed failure in gap detection leaves hashes unsaved so the next run re-analyzes. (Found in the 2026-07-02 cross-package audit.) <!-- completed_at: 2026-07-03T09:21:43Z -->
- [x] T-000002: Behavioral-target videos are re-extracted, re-Vision-filtered, and re-described on every subsequent run: when `spec and spec_sources`, `_run_video_frame_pipeline(behavioral_paths)` (pipeline.py ~1723-1736) runs unconditionally with no change gating, and if the video was also among the changed ref/ files it was already processed by `_analyze_new_files` earlier in the same run (~925) -- double Vision/LLM spend in a single invocation, plus duplicate `store_accepted_frames`. Gate on the hash diff like every other input and dedupe against paths already processed this run. Regression test: an unchanged behavioral video on a second run triggers zero extractor/describer calls. <!-- completed_at: 2026-07-03T09:37:21Z -->
- [x] T-000003: A real build task phrased "Verify ..." is misclassified as verification and then auto-deleted: `_VERIFY_PREFIX_RE` (plan_sanity.py ~46) treats any task starting with verify/test-that as verification, so its `[feat]` never enters `built_features`; `_check_verification_mapping` then flags KIND_VERIFY_WITHOUT_BUILD and `_repair_verify_without_build` (plan_gate.py ~103-124) removes the line -- silently deleting genuine feature work from PLAN.md. The repair gate must not delete tasks that carry a feat annotation (or the classifier must exempt them). Regression test: a task like "Verify the exporter handles empty input [feat: exporter]" survives the gate. <!-- completed_at: 2026-07-03T09:44:15Z -->
- [x] T-000004: The explicit `DUPLO_COUNCIL_CONFIG` branch (council.py ~683-694) skips the required-role check the project-config branch enforces, so a config missing council roles proceeds into a failing run_workflow instead of the graceful fallback; and `_ensure_council_workflow` (~740) rebuilds the config without `role_bindings`, silently dropping compound bindings whenever it injects the workflow entry. Apply the same `_COUNCIL_REQUIRED_ROLES` validation on the explicit path and preserve `role_bindings` in the rebuild; regression tests for both. Deeper cleanup worth considering in the same pass: load the explicit file directly via a public orchestra config-file loader instead of the load_config(project_dir) round-trip, which would also lift the hidden requirement that the file live at a path shaped like root/.orchestra/config.json. (The .parent-vs-.parent.parent bug on this path was already fixed in the 2026-07-02 audit.) <!-- completed_at: 2026-07-03T09:47:19Z -->
- [x] T-000005: `select_issues` (selector.py ~150) treats blank input as select-ALL (`if not raw or raw.lower() == "all"`), contradicting its own docstring ("blank -- include nothing (skip issues)"): pressing Enter to skip schedules every open issue. Also `iss["description"]` (~143) is indexed directly while every other field uses .get(), so one malformed issue dict crashes the whole interactive selection. Make blank mean none and use .get with a default. Regression tests for both. <!-- completed_at: 2026-07-03T09:48:33Z -->
- [x] T-000006: `extract_features` (extractor.py ~66) accepts a `scope_exclude` parameter it never uses, and the docstring (~87-88) claims both include and exclude lists are post-filtered -- false; only `scope_include` is applied via `_reconcile_scope_include`. The live caller re-filters separately with `_matches_excluded`, so the parameter is a trap for the next caller who trusts the docstring. Either wire the parameter or remove it and fix the docstring. <!-- completed_at: 2026-07-03T22:22:14Z -->
- [x] T-000007: Dead-code sweep from the 2026-07-02 audit, grep-verified: scanner.py `_SKIP_DIRS` (defined, never read -- scan_directory is deliberately top-level-only per test_subdirs_under_ref_ignored, so the skip set is vestigial), pipeline.py `_visual_target_video_frames` (~376-400, zero references, superseded by the inline path-keyed logic at ~1740), and the planner.py cluster reachable only from tests: `generate_next_phase_plan` (~450), `_detect_next_phase_number` (~444), `_ensure_h1_heading` (~241), `_strip_trailing_commentary` (~213), `append_test_tasks` (~652). Delete them and their orphaned tests, or document why they must stay. <!-- completed_at: 2026-07-03T22:31:11Z -->
- [x] T-000008: Small robustness fixes, suitable for one commit: pdf_extractor.py ~31 `PdfReader(path)` never closes its file handle (use a context manager); hasher.py ~82 `load_hashes` catches only JSONDecodeError so a truncated/binary/permission-broken hashes file crashes the run instead of the intended corrupt-to-empty fallback (catch OSError/UnicodeDecodeError too); comparator.py ~73 verdict parsing requires a bare token so "SIMILAR: yes." or "SIMILAR: Yes, they match" flips a positive verdict to False (strip punctuation or prefix-match); validator.py ~111 coerces `products` assuming a list, so an LLM string "Foo" iterates into ["F","o","o"] (add an isinstance guard). Regression test per fix. <!-- completed_at: 2026-07-03T22:32:40Z -->
